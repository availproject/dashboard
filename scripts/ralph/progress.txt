## Codebase Patterns
- Module path is `github.com/your-org/dashboard`
- Config loaded from `config.yaml` (YAML) via `gopkg.in/yaml.v3`
- Env vars loaded from `.env` via manual key=value parse in `internal/config/env.go` (no third-party library)
- Placeholder package files (single `package <name>` line) are used to satisfy directory requirements without real implementations
- `go vet ./...` is the typecheck command for this project
- `go build ./...` for full build verification
- Migration files use 4-digit prefix format: `0001_name.up.sql` / `0001_name.down.sql`
- Migrations embedded via `//go:embed migrations` in `internal/store/embed.go`; variable `migrationsFS embed.FS`
- golang-migrate wired with `database/sqlite` (modernc.org/sqlite, pure Go) + `source/iofs`; driver name is `"sqlite"` (sql.Open)
- nullable DB columns → `sql.NullString`, `sql.NullInt64`, `sql.NullTime` in model structs
- `Store.db` is unexported `*sql.DB`; use `store.New(path)` to open + migrate in one step
- Store query methods: use `ExecContext` + `LastInsertId` for INSERT (not RETURNING), then fetch by ID
- Test helper: use `t.TempDir() + "/test.db"` with `New()` for store tests (avoids `:memory:` connection-sharing issues with golang-migrate)
- Standard testing package (no testify); use `t.Fatal`/`t.Error` directly
- `scanMember` takes `*sql.Rows` (not `*sql.Row`) since team_members uses QueryContext for multi-row scans
- **TIME COMPARISON GOTCHA**: modernc/sqlite driver stores Go `time.Time` params in RFC 3339 format; SQLite's `CURRENT_TIMESTAMP` uses `"YYYY-MM-DD HH:MM:SS"`. These formats do NOT compare correctly with `<`/`>`. Fix: format Go time params explicitly with `.UTC().Format("2006-01-02 15:04:05")` when they must compare against `CURRENT_TIMESTAMP`
- **AI cache prune**: use `cutoff := time.Now().UTC().Add(-olderThan).Format("2006-01-02 15:04:05")` then `WHERE created_at <= ?` (note `<=` to include same-second entries when olderThan=0)
- **NULL-safe upsert for ai_cache**: SQLite UNIQUE with NULL team_id doesn't conflict; use SELECT-then-INSERT/UPDATE pattern (same as source_configs)
- **Bootstrap pattern**: `Bootstrap(st *store.Store, cfg *config.Config)` calls `st.ListUsers`, inserts admin only when empty; stores password hash directly (no re-hash)
- **Middleware context keys**: use unexported `contextKey` string type with constants `contextKeyUsername`/`contextKeyRole` to avoid collisions
- **RequireRole logic**: 'edit' satisfies any role; 'view' satisfies only 'view'; empty/unknown role denied always
- **go-github auth**: `gh.NewClient(nil).WithAuthToken(token)` — no oauth2 needed; alias import as `gh` since package name conflicts with our `github` package
- **go-github GetContent()**: `RepositoryContent.GetContent()` returns `(string, error)` and handles base64 decode automatically
- **git tree resolution**: `GetTree` needs real SHA; use `GetBranch → .GetCommit().GetCommit().GetTree().GetSHA()`
- **GraphQL pagination**: cursor type is `*string` (nil for first page); set to `&endCursor` for next pages
- **connector package**: shared `DiscoveredItem` + `Discoverer` interface live in `internal/connector/connector.go`
---

# Ralph Progress Log
Started: Fri Mar  6 16:09:22 CET 2026
---

## 2026-03-06 - US-001
- Initialized Go module `github.com/your-org/dashboard`
- Created full directory tree: cmd/server, cmd/tui, internal/{api,auth,config,connector/{github,notion,grafana,posthog,signoz},ai,pipeline,store,sync,tui}
- Implemented `internal/config/config.go` with Config/ServerConfig/StorageConfig/AuthConfig/AIConfig structs and Load()
- Implemented `internal/config/env.go` with manual .env parsing and token accessor functions
- Created `cmd/server/main.go` with --config flag, masked config summary output
- Created `cmd/tui/main.go` with --server flag
- Created `config.example.yaml` with all required fields
- Added all dependencies: chi, bubbletea, lipgloss, sqlite, golang-migrate, jwt/v5, x/crypto, yaml.v3, go-github/v60, notionapi
- Both binaries build and exit cleanly (exit code 0)
- Files changed: go.mod, go.sum, config.example.yaml, cmd/server/main.go, cmd/tui/main.go, internal/config/config.go, internal/config/env.go, internal/* placeholder files
- **Learnings for future iterations:**
  - All internal package dirs need at minimum a `package <name>` placeholder to build
  - `config.yaml` is gitignored-worthy (contains secrets) but `config.example.yaml` is the template
  - `golang-migrate` and `modernc.org/sqlite` are already in go.mod for future store work
---

## 2026-03-06 - US-002
- Created `internal/store/migrations/` with 16 files: 0001–0008 each with .up.sql and .down.sql
- Created `internal/store/embed.go` with `//go:embed migrations` and `var migrationsFS embed.FS`
- Schema: users, teams, team_members, source_catalogue, source_configs, annotations, ai_cache, sync_runs, sprint_meta, refresh_tokens
- Files changed: internal/store/embed.go, internal/store/migrations/0001_users.{up,down}.sql, 0002_teams.{up,down}.sql, 0003_source_catalogue.{up,down}.sql, 0004_source_configs.{up,down}.sql, 0005_annotations.{up,down}.sql, 0006_ai_cache.{up,down}.sql, 0007_sync_runs_sprint_meta.{up,down}.sql, 0008_refresh_tokens.{up,down}.sql
- `go vet ./...` and `go build ./...` pass
- **Learnings for future iterations:**
  - `migrationsFS` is exported from `internal/store/embed.go`; US-003 should use it with `iofs.New(migrationsFS, "migrations")` to wire into golang-migrate
  - Down migrations drop tables in reverse order of creation to respect FK constraints
  - `team_id` in ai_cache, annotations, source_configs, sync_runs is nullable (no NOT NULL) to allow org-level records
---

## 2026-03-06 - US-003
- Implemented `internal/store/store.go`: Store struct wrapping *sql.DB, New(path) opens DB + WAL + FK + migrations, Migrate(db) runner, Close()
- Implemented `internal/store/models.go`: User, Team, TeamMember, SourceCatalogue, SourceConfig, Annotation, AICache, SyncRun, SprintMeta, RefreshToken
- Added `github.com/golang-migrate/migrate/v4/database/sqlite` and `source/iofs` to go.mod via `go get`
- Files changed: internal/store/store.go, internal/store/models.go, go.mod, go.sum
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - `golang-migrate/v4/database/sqlite` (not sqlite3) is the pure-Go driver for modernc.org/sqlite; use `sqlite.WithInstance(db, &sqlite.Config{})` to get the driver
  - Migration source: `iofs.New(migrationsFS, "migrations")` where migrationsFS is the embed.FS from embed.go
  - `migrate.NewWithInstance("iofs", src, "sqlite", driver)` then `.Up()` ignoring `migrate.ErrNoChange`
  - nullable SQL columns: use `sql.NullString`/`sql.NullInt64`/`sql.NullTime` in model structs
---

## 2026-03-06 - US-004
- Implemented `internal/store/users.go`: CreateUser, GetUserByUsername, GetUserByID, UpdateUser, DeleteUser, ListUsers
- Implemented `internal/store/teams.go`: CreateTeam, UpdateTeam, DeleteTeam, ListTeams, AddMember, UpdateMember, DeleteMember, GetTeamMembers
- Created `internal/store/store_test.go` with CRUD round-trip tests for users and teams
- Files changed: internal/store/users.go, internal/store/teams.go, internal/store/store_test.go
- `go build ./...`, `go vet ./...`, and `go test ./internal/store/...` all pass
- **Learnings for future iterations:**
  - Use `ExecContext` + `LastInsertId` for INSERT then fetch by ID; avoids RETURNING compatibility concerns
  - Test store with `t.TempDir()/test.db` via `New()` — more reliable than `:memory:` with golang-migrate's connection management
  - `UpdateUser`/`UpdateTeam` return `sql.ErrNoRows` when RowsAffected == 0 (not-found sentinel)
  - `scanMember` helper takes `*sql.Rows` (not `*sql.Row`) since fetching by ID uses QueryContext + rows.Next()
  - No testify in go.mod; use standard `testing` package with `t.Fatal`/`t.Error`
---

## 2026-03-06 - US-006
- Implemented `internal/store/annotations.go`: CreateAnnotation, UpdateAnnotation, DeleteAnnotation, ListAnnotations, ArchiveItemAnnotationsForPlan
- Implemented `internal/store/ai_cache.go`: GetCacheEntry, SetCacheEntry, PruneStaleCache; uses SELECT-then-INSERT/UPDATE for NULL-safe upsert
- Implemented `internal/store/sync_runs.go`: CreateSyncRun (status='running'), UpdateSyncRun, GetSyncRun
- Implemented `internal/store/sprint_meta.go`: UpsertSprintMeta (ON CONFLICT DO UPDATE on team_id,plan_type), GetSprintMeta
- Implemented `internal/store/refresh_tokens.go`: CreateRefreshToken, GetRefreshTokenByHash, DeleteRefreshToken, DeleteExpiredRefreshTokens
- Added tests for all five table groups in `internal/store/store_test.go`
- Files changed: internal/store/annotations.go, ai_cache.go, sync_runs.go, sprint_meta.go, refresh_tokens.go, store_test.go
- `go build ./...`, `go vet ./...`, `go test ./internal/store/...` all pass (15 tests)
- **Learnings for future iterations:**
  - modernc/sqlite stores Go `time.Time` params in RFC 3339; compare against `CURRENT_TIMESTAMP` by formatting Go time as `t.UTC().Format("2006-01-02 15:04:05")`
  - PruneStaleCache(0) should delete everything including current-second entries; use `<=` not `<` in the comparison
  - Sprint_meta has UNIQUE(team_id, plan_type) with both NOT NULL → ON CONFLICT DO UPDATE works safely (unlike source_configs where team_id can be NULL)
  - ListAnnotations returns ALL annotations for a team (both archived and not), callers filter as needed
---

## 2026-03-06 - US-005
- Implemented `internal/store/catalogue.go`: UpsertCatalogueItem (INSERT ... ON CONFLICT DO UPDATE), ListCatalogue, GetCatalogueItem, UpdateCatalogueStatus, UpdateCatalogueAISuggestion
- Implemented `internal/store/source_configs.go`: UpsertSourceConfig, ListSourceConfigs, DeleteSourceConfig, GetSourceConfigsForScope
- Added catalogue and source config CRUD tests to `internal/store/store_test.go`
- Files changed: internal/store/catalogue.go, internal/store/source_configs.go, internal/store/store_test.go
- `go build ./...`, `go vet ./...`, and `go test ./internal/store/...` all pass
- **Learnings for future iterations:**
  - SQLite ON CONFLICT DO UPDATE (upsert) does not return LastInsertId reliably; after upsert, re-fetch by unique key to get the record
  - NULL team_id in UNIQUE(catalogue_id, team_id, purpose) makes ON CONFLICT DO UPDATE ineffective for org-level configs; use SELECT-then-INSERT pattern instead
  - GetSourceConfigsForScope uses conditional SQL: `WHERE team_id = ?` vs `WHERE team_id IS NULL` depending on NullInt64.Valid
  - scanCatalogue/scanSourceConfig use *sql.Rows (not *sql.Row) for consistency with getMemberByID pattern
---

## 2026-03-06 - US-007
- Implemented `internal/auth/password.go`: HashPassword (bcrypt cost 12), CheckPassword
- Implemented `internal/auth/jwt.go`: Claims struct, IssueToken (1h expiry, HS256), IssueRefreshToken (32 random bytes → raw hex + SHA-256 hex hash), ValidateToken
- Created `internal/auth/auth_test.go`: round-trip JWT, expired token rejection, password hash+check, refresh token uniqueness
- Files changed: internal/auth/password.go, internal/auth/jwt.go, internal/auth/auth_test.go
- `go build ./...`, `go vet ./...`, `go test ./internal/auth/...` all pass (4 tests)
- **Learnings for future iterations:**
  - golang-jwt/v5: use `jwt.ParseWithClaims` with `*Claims` pointer; check `token.Valid` after parsing
  - Refresh token pattern: rand.Read(32 bytes) → hex-encode for raw token; sha256.Sum256(rawToken) → hex-encode for stored hash
  - `jwt.RegisteredClaims` embeds in custom Claims struct; set both `Sub` field and `RegisteredClaims.Subject` for compatibility
---

## 2026-03-06 - US-008
- Implemented `internal/auth/bootstrap.go`: Bootstrap(store, cfg) checks if users table is empty via ListUsers; if empty, creates admin user with username and pre-hashed password from config, role='edit'
- Implemented `internal/api/middleware.go`: RequireAuth(secret) validates Bearer JWT, sets contextKeyUsername and contextKeyRole in request context (typed contextKey string type); RequireRole(role) checks context role with 'edit' satisfying any requirement
- Created `internal/api/middleware_test.go`: tests RequireAuth (missing token, invalid token, valid token sets context), RequireRole (6 cases covering edit/view combinations)
- Files changed: internal/auth/bootstrap.go, internal/api/middleware.go, internal/api/middleware_test.go
- `go vet ./...`, `go build ./...`, `go test ./internal/auth/... ./internal/api/...` all pass
- **Learnings for future iterations:**
  - Use unexported `contextKey` string type for context keys to avoid collisions with other packages
  - Bootstrap directly stores pre-hashed passwords (no double-hashing); the hash in config was already produced by `hash-password` subcommand
  - roleAllowed: 'edit' role passes all checks; 'view' only passes 'view'; anything else fails
---

## 2026-03-06 - US-009
- Implemented `internal/api/response.go`: writeJSON, writeError, readJSON (1MB limit via io.LimitReader)
- Implemented `internal/api/auth.go`: POST /auth/login (verify password → issue JWT + refresh token), POST /auth/refresh (hash lookup → rotate token → issue new pair)
- Implemented `internal/api/router.go`: NewRouter(deps *Deps); Deps struct with Store, Config, Engine (SyncEngine interface), Pipeline (PipelineRunner interface); chi router with public auth routes + RequireAuth group + RequireRole('edit') sub-group
- Created stub handlers returning 501: org.go, teams.go, config_teams.go, config_sources.go, config_annotations.go, config_users.go, sync.go, annotations.go
- Updated `cmd/server/main.go`: hash-password subcommand (os.Args[1] check before flag.Parse), store open+migrate, bootstrap, Deps wiring, http.ListenAndServe
- Files changed: internal/api/response.go, auth.go, router.go, org.go, teams.go, config_teams.go, config_sources.go, config_annotations.go, config_users.go, sync.go, annotations.go, cmd/server/main.go
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - `auth.CheckPassword` returns `bool`, not `error` — use `if !auth.CheckPassword(...)` pattern
  - `http.MaxBytesReader` requires a ResponseWriter; use `io.LimitReader` instead when only the request is available
  - chi router: group mutations under nested `r.Group` with `RequireRole('edit')` middleware inside the `RequireAuth` group
  - hash-password subcommand must check `os.Args[1]` before `flag.Parse()` to avoid flag parsing errors
---

## 2026-03-06 - US-010
- Created `internal/connector/connector.go`: DiscoveredItem struct + Discoverer interface
- Implemented `internal/connector/github/github.go`: Client wrapping go-github v60 with token transport
  - New(token) returns *Client; empty token deferred to runtime (methods return "GITHUB_TOKEN missing" error)
  - Discover(ctx, target): enumerates labels (paginated REST), ProjectsV2 (GraphQL), .md files (git tree API)
  - FetchIssues: search API with q=repo:owner/repo+label:label+updated:>date
  - FetchDraftIssues: GraphQL ProjectV2Items filtered by type=DRAFT_ISSUE and Team/Area field value
  - FetchMergedPRs: list closed PRs, filter client-side by merged_at > since
  - FetchCommits: ListCommits with since/until, filter client-side by author login set
  - FetchMarkdownFile: GetContents + go-github's GetContent() for automatic base64 decode
  - AutoTagIssues: GraphQL page all project items; apply label via REST when Team/Area set but label missing
  - graphql() helper: plain net/http POST to https://api.github.com/graphql with Bearer token
- Added `github.com/google/go-github/v60` as direct dep (was indirect); go get resolved go-querystring transitive dep
- Files changed: internal/connector/connector.go, internal/connector/github/github.go, go.mod, go.sum
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - go-github v60: `gh.NewClient(nil).WithAuthToken(token)` is the idiomatic token auth pattern (no oauth2 needed)
  - go-github `RepositoryContent.GetContent()` returns `(string, error)` and handles base64 decode automatically
  - go-github `GetTree` with `recursive=true` needs a real tree SHA (not "HEAD"); resolve via `GetBranch` → commit → tree SHA
  - GraphQL pagination: use `cursor *string` (nil for first page), set to `&endCursor` for subsequent pages
  - Package name `github` conflicts with `go-github/v60/github`; alias the import as `gh`
  - go-github was `// indirect` in go.mod; first direct use requires `go get` to update go.sum with transitive deps
---

## 2026-03-06 - US-011
- Implemented `internal/connector/notion/notion.go`:
  - `New(token string) *Client` wrapping `jomei/notionapi`; empty token returns Client that fails at call time
  - `Discover(ctx, workspaceHint)` paginates `Search.Do` with empty query; maps `*notion.Page` → `notion_page` and `*notion.Database` → `notion_db` DiscoveredItems
  - `FetchPage(ctx, pageID)` calls `Page.Get` for metadata, then `fetchBlocksText` recursively for content; returns (content, lastEditedTime, err)
  - `FetchDatabase(ctx, dbID, updatedAfter)` queries with `TimestampFilter{Timestamp: TimestampLastEdited}`, fetches block content per page row
  - `fetchBlocksText` recursively paginates `Block.GetChildren`, calls `blockToText` per block, recurses on `HasChildren`
  - `blockToText` maps: Heading1/2/3 → `# text`, BulletedList/NumberedList → `- text`, Paragraph → plain text, Toggle → `> text`
- Files changed: internal/connector/notion/notion.go
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - `notion.TimestampLastEdited` (not `TimestampLastEditedTime`) is the const in jomei/notionapi v1.13.3 (`const.go`)
  - `notion.Date` is `type Date time.Time`; cast with `(*notion.Date)(timePtr(t))` for filter conditions
  - `SearchResponse.Results` is `[]Object` (interface); type-switch on `*notion.Page` / `*notion.Database`
  - Page title: iterate `page.Properties` (map[string]Property), type-assert to `*TitleProperty`
  - Database title: `db.Title` is `[]RichText` directly (not in Properties map)
  - `Block.GetChildren` pagination: pass `*notion.Pagination{StartCursor: cursor}` or nil for first page
  - jomei/notionapi was already in go.mod as indirect; no `go get` needed
---

## 2026-03-06 - US-018
- Implemented `internal/pipeline/runner.go`: Runner struct with *ai.CachedGenerator and *store.Store
- New(gen, store) constructor; activeAnnotations() helper filters archived==0 from ListAnnotations
- Internal generate() helper: builds prompt, calls gen.Generate, unmarshals JSON → returns ErrInvalidAIOutput on parse failure
- Methods: RunSprintParse, RunGoalExtraction, RunConcerns, RunWorkloadEstimation, RunVelocityAnalysis, RunGoalAlignment, RunDiscoverySuggestion
- RunSprintParse additionally calls store.UpsertSprintMeta after successful parse
- Input types defined: ConcernsInput, WorkloadInput, VelocityInput
- Created `internal/pipeline/runner_test.go`: integration test skips when `claude` binary not in PATH
- Integration test uses `ai.New(config.AIConfig{Provider: "claude-code"})` factory (newClaudeCodeProvider is unexported)
- Files changed: internal/pipeline/runner.go, internal/pipeline/runner_test.go
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - `newClaudeCodeProvider` is unexported; use `ai.New(config.AIConfig{Provider: "claude-code"})` factory in tests
  - `store.ListAnnotations` returns ALL annotations (not just active); filter client-side with `a.Archived == 0`
  - Runner methods implement pipeline logic directly (not via package-level Run* functions) for precise ErrInvalidAIOutput control
  - `ErrInvalidAIOutput` sentinel lives in `internal/pipeline/runner.go`
---

## 2026-03-06 - US-019
- Implemented `internal/sync/engine.go`: Engine struct with store, github/notion/grafana/posthog/signoz clients, pipeline runner; New(...) constructor
- Implemented `internal/sync/discover.go`: Discover(ctx, scope, target) creates sync_run, launches goroutine
- Goroutine: routes to notion/github/metrics connectors by scope; upserts each DiscoveredItem; runs RunDiscoverySuggestion for items with no AI suggestion; marks run completed/failed
- `metrics_url` scope tries all three metrics connectors (grafana, posthog, signoz), silently ignoring individual connector errors
- Files changed: internal/sync/engine.go, internal/sync/discover.go
- `go build ./...` and `go vet ./...` pass
- **Learnings for future iterations:**
  - `internal/sync` package is named `sync`; no stdlib sync import needed in these files (goroutines launched with `go`, no WaitGroup)
  - Import alias needed for github connector: `ghconn "github.com/your-org/dashboard/internal/connector/github"` to avoid ambiguity
  - Detect "new" catalogue items by checking `AISuggestion.Valid == false`; new items from UpsertCatalogueItem won't have a suggestion yet
  - For `metrics_url` scope: try all three connectors, ignore per-connector errors (URL may only match one service)
  - Goroutine uses `context.Background()` (not parent ctx) to avoid cancellation when HTTP request completes
---
