server:
  port: 8081

storage:
  path: ./dashboard.db

auth:
  jwt_secret: changeme
  admin_username: admin
  admin_password_hash: ""

ai:
  provider: anthropic
  model: claude-opus-4-6
  api_key: ""
  binary_path: claude

mcp:
  api_key: ""   # static Bearer token for /mcp endpoint; leave empty to disable auth
